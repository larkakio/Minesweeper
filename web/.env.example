# Site (Vercel production URL — used for metadataBase / Open Graph)
NEXT_PUBLIC_SITE_URL=https://minesweeper-eight-delta.vercel.app

# Chain (Base mainnet)
NEXT_PUBLIC_CHAIN_ID=8453

# Daily check-in contract (deploy contracts/ then paste address)
NEXT_PUBLIC_CHECK_IN_CONTRACT_ADDRESS=0x

# Base.dev — meta base:app_id (dashboard → app settings)
NEXT_PUBLIC_BASE_APP_ID=6a057fdbacef3c7a49b1600e

# Builder code from base.dev → Settings → Builder Codes (override default in code)
NEXT_PUBLIC_BUILDER_CODE=bc_3qjnseuu

# Rare override: raw 0x suffix instead of deriving from NEXT_PUBLIC_BUILDER_CODE
# NEXT_PUBLIC_BUILDER_CODE_SUFFIX=0x

# Optional WalletConnect (not installed by default in this template)
# NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
